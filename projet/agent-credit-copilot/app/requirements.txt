# Credit Copilot — dépendances (self-contained : installables dans un venv frais)
#   python -m pip install -r projet/agent-credit-copilot/app/requirements.txt

# --- Cœur agentique (TD4/TD5) --------------------------------------------------
anthropic
mcp

# --- RAG / embeddings (TD1/TD3) ------------------------------------------------
chromadb
sentence-transformers

# --- Modèle ML + explicabilité (TD2) -------------------------------------------
scikit-learn
xgboost
shap
pandas
numpy
joblib

# --- Interface web (UI principale) ---------------------------------------------
fastapi
uvicorn
pymupdf          # rendu des pages PDF (fitz)
python-dotenv

# --- Génération des données de démo (scripts/) ---------------------------------
faker
reportlab
pypdf

# --- UI alternative, optionnelle (ancienne version) ----------------------------
streamlit
