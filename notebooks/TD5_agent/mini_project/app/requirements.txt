# PIM Copilot backend -- install with:  pip install -r app/requirements.txt
fastapi>=0.110
uvicorn[standard]>=0.27
python-dotenv>=1.0
anthropic>=0.40
mcp>=1.0
# The spawned TD4 server also needs these (already in the repo venv):
chromadb>=0.5
sentence-transformers>=2.5
pandas>=2.0
